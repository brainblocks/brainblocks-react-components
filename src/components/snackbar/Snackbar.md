Example:

```js
const styles = require('./snackbar.style').default
const addStyles = require('destyle').addStyles
addStyles('BB-Snackbar', props => styles(props))
const Button = require('../button/Button').default
;<div>@Todo</div>
```
